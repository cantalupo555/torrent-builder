set(TORRENT_BUILDER_VERSION "1.2.3")
include(${CMAKE_CURRENT_LIST_DIR}/../../cmake/version.cmake)
if(NOT TORRENT_BUILDER_VERSION STREQUAL "1.2.3")
    message(FATAL_ERROR "FAIL: VERSION should be 1.2.3, got: ${TORRENT_BUILDER_VERSION}")
endif()
if(NOT TORRENT_BUILDER_DISPLAY_VERSION STREQUAL "1.2.3")
    message(FATAL_ERROR "FAIL: DISPLAY_VERSION should be 1.2.3, got: ${TORRENT_BUILDER_DISPLAY_VERSION}")
endif()
message(STATUS "PASS: version override accepted")
